## 1. Runner

- [x] 1.1 Add bounded selected-owner feed reads and verify success, wrong-owner, malformed, oversized and timeout responses with loopback tests.
- [x] 1.2 Add private configuration loading, a per-device lease and CLI shutdown; verify unsafe files, duplicate processes, process-death release and queue behavior.

## 2. Delivery

- [x] 2.1 Document installation, startup, stop/recovery and the real-client visual sequence; inspect commands against the delivered CLI.
- [x] 2.2 Run the Tidbyt, shared build/type/contract and workflow checks; record source-only evidence separately from installation acceptance.

Physical execution and the user's closing acceptance stay in issue #21; these tasks deliver the supporting source and runbook.
