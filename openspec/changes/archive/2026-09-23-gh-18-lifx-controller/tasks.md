## 1. Package and protocol

- [x] 1.1 Add workspace/build/type/test commands, development instructions and CI before product code; verify npm ci and command wiring.
- [x] 1.2 Implement bounded direct UDP packet handling; verify fake-socket tests for bytes, correlation, abort and close with no native sockets.

## 2. Controller

- [x] 2.1 Add strict profile and qualified capability mapping; verify unsupported and malformed requests produce no traffic and v1 receipts/snapshots validate.
- [x] 2.2 Implement per-bulb queues, bounded retries, replay, generation cancellation and close; preserve a focused red result and verify timeout, lost reply and overlap tests pass.
- [x] 2.3 Add dated read observations and independent batch outcomes; verify partial failure, preservation of HSBK and observation-age tests.

## 3. Integration

- [x] 3.1 Update owning documentation and instruction status without installation claims; inspect links and run workflow checks.
- [x] 3.2 Run root build/type, LIFX and affected shared consumer checks; synchronize the completed spec and verify workflow inventory before archive.
