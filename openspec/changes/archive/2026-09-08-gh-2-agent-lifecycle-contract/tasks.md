## 1. Qualification and executable contract

- [x] 1.1 Record accessible installed artifacts, primary-source event/identity/delivery evidence, Windows/WSL compatibility gaps and required-client stop conditions in the provider matrix; inspect it against the live issue.
- [x] 1.2 Add strict lifecycle validation and a shared corpus through focused red/green cycles; verify privacy rejection, unsupported versions, identity, event dimensions and deterministic fallback in TypeScript and Python.
- [x] 1.3 Document consumer notice/acknowledgment/read policies, freshness and bounded fail-open delivery; map each issue criterion to fixtures or explicit qualification evidence.

## 2. Distribution and integration checks

- [x] 2.1 Add reproducible versioned archive packaging and verify manifest hashes plus both language consumers installed outside the checkout.
- [x] 2.2 Add documented lifecycle build/type/test/package CI commands, retain existing jobs and pass the shared workflow/contract/MCP checks with Node 24 and supported Python.
- [x] 2.3 Maintain the work-guide owning inputs and dated update, regenerate and pass its three canonical checks while preserving the pinned #73 evaluation input and recording any diagram changes.

## 3. Candidate preparation

- [x] 3.1 Validate source acceptance evidence and prepare the synchronized lifecycle spec for archival before the committed independent-review candidate; record pending installed-client qualification separately.
