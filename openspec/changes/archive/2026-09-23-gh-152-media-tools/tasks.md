## 1. Media bindings

- [x] 1.1 Demonstrate the missing media tools with a failing host MCP forwarding test.
- [x] 1.2 Register both tools through the existing guarded helper and pass the forwarding test for playlist start and all controller v1 playback actions.
- [x] 1.3 Verify strict inputs, typed owner failures, authorization changes, replay and ambiguous writes without automatic retries using fake loopback controllers.

## 2. Documentation and validation

- [x] 2.1 Document the tool inputs, explicit Pixoo Media selection and existing source/package coverage.
- [x] 2.2 Pass shared build/type, controller, lifecycle, agent-state, MCP, host, setup, package and workflow checks; retain evidence outside the commit.
- [x] 2.3 Obtain current OpenSpec lookups, synchronize the standalone-hub-mcp delta and archive the completed change before independent review.
