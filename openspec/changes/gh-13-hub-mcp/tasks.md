## 1. Host transport and session services

- [ ] 1.1 Add a focused failing host MCP initialization test, then mount the opt-in reusable handler with shared credential verification; verify disabled/auth/origin and both protocol profiles.
- [ ] 1.2 Share session inspection and state commands with HTTP; verify qualified evidence, no read effects, cross-transport replay/conflict, revocation and staged-owner rejection.

## 2. Configured controller tools

- [ ] 2.1 Bind alias discovery and native status/power/brightness/mode/media tools through existing clients; verify native identities, authorization and unsupported capability with fakes.
- [ ] 2.2 Bind typed Pixoo and Nanoleaf integration snapshots, commands and supported receipt/cancellation operations; verify native revisions, results and HTTP/MCP coordination.
- [ ] 2.3 Verify independently failing controllers, bounded concurrent callers and disconnect after dispatch without retries or fabricated prior-effects claims.

## 3. Packaging and delivery evidence

- [ ] 3.1 Bundle the reusable MCP dependency closure and run offline installed host tests plus shared build/type/contract/MCP/workflow checks.
- [ ] 3.2 Update host/development documentation and the affected work-guide inputs/output; record source-only client gaps and run guide checks.
- [ ] 3.3 Validate complete planning artifacts, synchronize the new capability and archive before the fixed-comparison independent reviews.
